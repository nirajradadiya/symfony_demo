{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Add Company</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            
            <button type="submit" class="btn btn-default">Create</button>
            <button type="button" class="btn btn-link"><a href="{{ path('company_index') }}">Back to the list</a></button>
        {{ form_end(form) }}
    </div>
{% endblock %}
